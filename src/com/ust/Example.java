package com.ust;

import java.util.concurrent.ConcurrentHashMap;

public class Example {
    /*Design and implement a basic, thread-safe, in-memory key-value store in Java. The store should have the following features:

put(key, value): Inserts or updates a key-value pair.
get(key): Retrieves the value for a given key.
delete(key): Deletes a key-value pair.
Time-to-live (TTL): Support an optional TTL for each key. The key-value pair should be automatically removed from the store after the specified TTL expires.
The core challenge lies in making the store thread-safe and handling the expiration of keys efficiently.*/
}


